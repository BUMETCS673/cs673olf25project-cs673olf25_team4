name: Deploy to EC2

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  deploy:
    if: >
      ${{
        github.event.workflow_run.conclusion == 'success' &&
        github.event.workflow_run.head_branch == 'main'
      }}
    runs-on: ubuntu-latest
    env:
      JAMBASE_API_KEY: ${{ secrets.JAMBASE_API_KEY }}
      TM_API_SECRET: ${{ secrets.TM_API_SECRET }}
      TM_API_KEY: ${{ secrets.TM_API_KEY }}
      GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
      
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          ref: main   # Always checkout main

      - name: Deploy via SSH using deploy key
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_DEPLOY_KEY }}
          script: |
            export JAMBASE_API_KEY=${{ secrets.JAMBASE_API_KEY }}
            export TM_API_SECRET=${{ secrets.TM_API_SECRET }}
            export TM_API_KEY=${{ secrets.TM_API_KEY }}
            export GROQ_API_KEY=${{ secrets.GROQ_API_KEY }}
            echo "Starting deployment of main branch..."

            echo "Checking if Git is installed..."
            if ! command -v git &> /dev/null
            then
              echo "Git not found. Installing Git..."
              sudo yum update -y
              sudo yum install -y git
            else
              echo "Git already installed."
            fi

            echo "Changing to home directory..."
            cd /home/ec2-user

            echo "Checking for running containers..."
            if [ "$(docker ps -q)" ]; then
              echo "Stopping existing containers..."
              sudo docker-compose -f cs673olf25project-cs673olf25_team4/src/docker-compose.yml down
              echo "Containers stopped."
            else
              echo "No running containers found."
            fi
            
            echo "Checking for old repository..."
            if [ -d "cs673olf25project-cs673olf25_team4" ]; then
              echo "Removing old repository..."
              rm -rf cs673olf25project-cs673olf25_team4
              echo "Old repository removed."
            else
              echo "No old repository found."
            fi

            echo "Wait..."
            sleep 3
            echo "Cloning fresh copy of repository (branch: main)..."
            git clone --branch main git@github.com:BUMETCS673/cs673olf25project-cs673olf25_team4.git
            echo "Repository cloned."

            echo "Moving into project src directory..."
            cd cs673olf25project-cs673olf25_team4/src || exit 1

            echo "Rebuilding Docker containers from scratch..."
            sudo -E docker-compose build --no-cache
            echo "Docker images built."

            echo "Starting Docker containers..."
            sudo -E docker-compose up -d
            echo "Deployment complete."
