# ===============================
# BeatMap Frontend - HTTPS config
# ===============================

# Redirect all HTTP → HTTPS
server {
    listen 80;
<<<<<<< HEAD
    server_name localhost;
=======
    server_name beatmap.live www.beatmap.live;

    return 301 https://$host$request_uri;
}

# Main HTTPS server
server {
    listen 443 ssl;
    server_name beatmap.live www.beatmap.live;

    # SSL certificates (mounted from host)
    ssl_certificate     /etc/ssl/beatmap/fullchain.pem;
    ssl_certificate_key /etc/ssl/beatmap/privkey.pem;

    # Optional: harden SSL
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers HIGH:!aNULL:!MD5;
>>>>>>> main

    root /usr/share/nginx/html;
    index index.html;

<<<<<<< HEAD
    # React routes
=======
    # Use Docker's internal DNS resolver for dynamic backend container discovery
    resolver 127.0.0.11 valid=30s ipv6=off;
    set $backend_upstream https://backend:8443;

    # Serve the SPA
>>>>>>> main
    location / {
        try_files $uri $uri/ /index.html;
    }

<<<<<<< HEAD
    # Proxy API calls to FastAPI backend
    location /concerts/ {
        proxy_pass http://concert_backend:8000/concerts/;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
=======
    # Proxy API calls to backend
    location /api/ {
        proxy_pass $backend_upstream/api/;
>>>>>>> main
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_ssl_verify off;  # because backend uses self-signed or internal cert
    }

<<<<<<< HEAD
    location /nl-concerts/ {
        proxy_pass http://concert_backend:8000/nl-concerts/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
=======
    # Optional: proxy specific endpoints
    location /concerts {
        proxy_pass $backend_upstream/concerts;
        proxy_ssl_verify off;
    }

    location /nl-concerts {
        proxy_pass $backend_upstream/nl-concerts;
        proxy_ssl_verify off;
>>>>>>> main
    }

    # Fallback error page
    error_page 497 500 502 503 504 /index.html;
}
